const Sql = require('../lib/Queries');

// test('creates a SELECT * FROM ? statement', () => {
//   const obj = { tableName: 'employee' }
//   expect(new Sql(obj).viewTable()).toBe('SELECT * FROM employee');
// });